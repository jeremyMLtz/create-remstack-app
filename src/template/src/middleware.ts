// import { authMiddleware } from "@clerk/nextjs";

// export default authMiddleware();

// export const config = {
//   matcher: ["/((?!.+\\.[\\w]+$|_next).*)", "/", "/(api|trpc)(.*)"],
// };
// ONCE CLERK TOKENS ARE SET UP, UNCOMMENT THE ABOVE CODE AND REMOVE doNothing()
export default function doNothing() {}
